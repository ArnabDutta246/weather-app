import { RegionCityDividarPipe } from './region-city-dividar.pipe';

describe('RegionCityDividarPipe', () => {
  it('create an instance', () => {
    const pipe = new RegionCityDividarPipe();
    expect(pipe).toBeTruthy();
  });
});
