import { TempareturePipe } from './tempareture.pipe';

describe('TempareturePipe', () => {
  it('create an instance', () => {
    const pipe = new TempareturePipe();
    expect(pipe).toBeTruthy();
  });
});
